Ext.define('Form.view.email.emailView', { 
	extend: 'Ext.form.Panel',
	alias: 'widget.emailview',
	title: 'e-mail',
	width: '100%', 
	height: '100%',
	autoScroll: true,
	initComponent: function() {   
		this.callParent(arguments);
	}
});